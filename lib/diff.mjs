const fields=["title","department","team","location","country","workplace","employmentType","publishedAt","updatedAt","applyUrl","descriptionHash","salary"];
const stable=value=>JSON.stringify(value??null);
export function diffJobs(previous,current,occurredAt){const before=new Map(previous.map(job=>[job.id,job])),after=new Map(current.map(job=>[job.id,job])),events=[];for(const id of [...new Set([...before.keys(),...after.keys()])].sort()){const oldJob=before.get(id),newJob=after.get(id);if(!oldJob)events.push(event("JOB_POSTED",newJob,occurredAt));else if(!newJob)events.push(event("JOB_CLOSED",oldJob,occurredAt));else for(const field of fields)if(stable(oldJob[field])!==stable(newJob[field]))events.push(event(typeFor(field),newJob,occurredAt,field,oldJob[field],newJob[field]))}return events}
const typeFor=field=>({title:"TITLE_CHANGED",location:"LOCATION_CHANGED",country:"LOCATION_CHANGED",workplace:"WORKPLACE_CHANGED",salary:"SALARY_CHANGED",descriptionHash:"DESCRIPTION_CHANGED"})[field]??"JOB_UPDATED";
const event=(type,job,occurredAt,field=null,before=null,after=null)=>({id:`${occurredAt}:${job.id}:${type}:${field??"job"}`,type,jobId:job.id,company:job.company,companyName:job.companyName,field,before,after,occurredAt,evidence:{url:job.sourceUrl}});
